#include <stdio.h>
#include <string.h>

int main() {
	int N;
	char str[100];

	while (scanf("%d", &N)) {
		if (!N) {
			break;
		}

		sprintf(str, "%d", N);
		//printf("B%sE\n", str);

		int len = strlen(str);
	}

	return 0;
}
