#include <string>
#include <iostream>
#include <regex>
#include <cstdio>

int main()
{
    std::string str;
    std::regex r("(\\d+)\\s+(\\d+)");
    while (true)
    {
        getline(std::cin, str);
        std::smatch mch;
        regex_search(str, mch, r);

        int n = std::stoi(mch[1]);
        int m = std::stoi(mch[2]);

        if (!n)
        {
            break;
        }

        printf("n=%d m=%d\n", n, m);

        for (int i = 0; i < m; ++i)
        {
            getline(std::cin, str);
            std::smatch mch;
            regex_search(str, mch, r);

            int first  = std::stoi(mch[1]);
            int second = std::stoi(mch[2]);
        }
    }
    
    return 0;
}
