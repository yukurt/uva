#include <stdio.h>

int main() {
  char line[100];
  int n, a, f;
 
  fgets(line, 100, stdin);
  sscanf(line, "%d", &n);

  for (int i = 0; i < n; i += 1) {
    fgets(line, 100, stdin);

    fgets(line, 100, stdin);
    sscanf(line, "%d", &a);
    
    fgets(line, 100, stdin);
    sscanf(line, "%d", &f);

    for (int j = 0; j < f; j += 1) {
      
    }
  }

  return 0;
}
